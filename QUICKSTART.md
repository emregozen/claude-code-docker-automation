# Quick Start (5 min)

1. Get keys:
   - https://console.anthropic.com/settings/keys
   - https://github.com/settings/tokens

2. Setup:
```bash
   cp .env.example .env
   nano .env  # Add keys
```

3. Run:
```bash
   ./run-claude.sh "Fix linting errors"
```

Done! Check GitHub for your PR.
